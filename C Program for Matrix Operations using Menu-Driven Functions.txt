// C Program for Matrix Operations using Menu-Driven Functions

#include<stdio.h>
void read_matrix(int mat[2][2],int,int);
void sum_matrix(int mat1[2][2],int mat2[2][2],int,int);
void mat_matrix(int mat1[2][2],int mat2[2][2],int,int);
void transpose_matrix(int mat2[2][2],int,int);
void display_matrix(int mat[2][2],int,int);
int main()
{
    int option,row,col;
    int mat1[2][2],mat2[2][2];
    do
    {
        printf("\n ******MAIN MENU*");
        printf("\n1.Read two matrices");
        printf("\n2.Add two matrices");
        printf("\n3.Multiply two matrices");
        printf("\n4. Transpose the matrix");
        printf("\n5. Exit");
        printf("\n\n Enter your option:");
        scanf("%d",&option);
        switch(option)
        {
            case 1:
            printf("\n Enter the number of rows and clumns of a matrix:");
            scanf("%d%d",&row,&col);
            printf("\n Enter the first matrix:");
            read_matrix(mat1,row,col);
            printf("\n enter the second matrix:");
            read_matrix(mat2,row,col);
            break;
            case 2:
            sum_matrix(mat1,mat2,row,col);
            break;
            case 3:
            if(row==col)
            mat_matrix(mat1,mat2,row,col);
            else
            printf("\n To multiply hoe matrices the number ofcloumns in the first matrix should be equal to the number of rows in the second matrix");
            break;
            case 4:
            transpose_matrix(mat1,row,col);
            break;
        }
    }while(option<=4);
    return 0;
}
    



    void read_matrix(int mat[2][2],int r,int c)
    {
        int i,j;
        for(i=0;i<r;i++)
        {
            for(j=0;j<c;j++)
            {
                printf("\t mat[%d][%d]=",i,j);
                scanf("%d",&mat[i][j]);
                
            }
        }
    }
    void sum_matrix(int mat1[2][2],int mat2[2][2],int r,int c)
    {
        int i,j,sum[2][2];
        for(i=0;i<r;i++)
        {
            for(j=0;j<c;j++)
            {
                sum[i][j]=mat1[i][j]+mat2[i][j];
            }
        }
        display_matrix(sum,r,c);
    }
    void mat_matrix(int mat1[2][2],int mat2[2][2],int r,int c)
    {
        int i,j,k,prod[2][2];
        for(i=0;i<r;i++)
        {
            for(j=0;j<c;j++)
            {
                prod[i][j]=0;
                for(k=0;k<c;k++)
                {
                    prod[i][j]+=mat1[i][k]*mat2[k][j];
                }
            }
        }
        display_matrix(prod,r,c);
    }
    void transpose_matrix(int mat[2][2],int r,int c)
    {
        int i,j,tp_mat[2][2];
        for(i=0;i<r;i++)
        {
            for(j=0;j<c;j++)
            {
                tp_mat[j][i]=mat[i][j];
            }
        }
        display_matrix(tp_mat,r,c);
    }
    void display_matrix(int mat[2][2],int r,int c)
    {
    int i,j;
    for(i=0;i<r;i++)
    {
        for(j=0;j<c;j++)
        {
            printf("\t mat[%d][%d]=%d",i,j,mat[i][j]);
        }
        printf("\n");
    }
    
}


Output:
 ******MAIN MENU*
1.Read two matrices
2.Add two matrices
3.Multiply two matrices
4. Transpose the matrix
5. Exit

 Enter your option: 1

 Enter the number of rows and clumns of a matrix: 2 2
 Enter the first matrix:
	 mat[0][0]=1
	 mat[0][1]=2
	 mat[1][0]=3
	 mat[1][1]=4
 enter the second matrix:
	 mat[0][0]=5
	 mat[0][1]=6
	 mat[1][0]=7
	 mat[1][1]=8

 ******MAIN MENU*
1.Read two matrices
2.Add two matrices
3.Multiply two matrices
4. Transpose the matrix
5. Exit

 Enter your option: 2
	 mat[0][0]=6	 mat[0][1]=8
	 mat[1][0]=10	 mat[1][1]=12

 ******MAIN MENU*
1.Read two matrices
2.Add two matrices
3.Multiply two matrices
4. Transpose the matrix
5. Exit

 Enter your option: 3
	 mat[0][0]=19	 mat[0][1]=22
	 mat[1][0]=43	 mat[1][1]=50

 ******MAIN MENU*
1.Read two matrices
2.Add two matrices
3.Multiply two matrices
4. Transpose the matrix
5. Exit

 Enter your option: 4
	 mat[0][0]=1	 mat[0][1]=3
	 mat[1][0]=2	 mat[1][1]=4
