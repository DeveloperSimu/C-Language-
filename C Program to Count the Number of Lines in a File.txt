// C Program to Count the Number of Lines in a File

#include<stdio.h>
#define MAX_FILE_NAME 100
int main()
{FILE *fp;
int count=0;
char filename[MAX_FILE_NAME];
char c;
printf("\n Enter file name:");
scanf("%s", filename);
fp=fopen(filename,"r");
if(fp==NULL)
{printf("Could not open the file");
return 0;
}
for(c=getc(fp);c!=EOF;c=getc(fp))
if(c=='\n')
count=count+1;
fclose(fp);
printf("\n The file %s has %d lines", filename, count);
return 0;
}


Output:
Enter file name: sample.txt
The file sample.txt has 5 lines
